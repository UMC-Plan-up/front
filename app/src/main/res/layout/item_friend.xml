<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/white"
    android:padding="16dp">

    <!-- 프로필 이미지 -->
    <ImageView
        android:id="@+id/profile_iv"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@drawable/circle_bg"
        android:scaleType="centerCrop"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- 텍스트 영역 -->
    <LinearLayout
        android:id="@+id/friend_profile_ll"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:orientation="vertical"
        app:layout_constraintEnd_toStartOf="@id/arrow_right_ic"
        app:layout_constraintStart_toEndOf="@id/profile_iv"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/nickname_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="16sp"
            android:textStyle="bold"
            tools:text="@string/friend_friend_name" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/goal_count_tv"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="14sp"
                tools:text="@string/friend_goal_count" />

        </LinearLayout>

    </LinearLayout>

    <!-- 오른쪽 화살표 -->
    <ImageView
        android:id="@+id/arrow_right_ic"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginTop="10dp"
        android:src="@drawable/ic_arrow_right"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- 오늘 진행 시간 & 사진 인증 하단 섹션 -->

    <LinearLayout
        android:id="@+id/friend_info_ll"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="14dp"
        android:gravity="center"
        android:orientation="horizontal"
        app:layout_constraintStart_toStartOf="@id/profile_iv"
        app:layout_constraintTop_toBottomOf="@id/profile_iv">

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/today_time_cv"
            android:layout_width="0dp"
            android:layout_height="37dp"
            android:layout_weight="1"
            app:cardBackgroundColor="#FFFFFF"
            app:cardCornerRadius="10dp"
            app:cardElevation="2dp"
            app:cardPreventCornerOverlap="false"
            app:cardUseCompatPadding="true"
            app:layout_constraintStart_toStartOf="@id/profile_iv"
            app:layout_constraintTop_toBottomOf="@id/friend_profile_ll">

            <!-- 진행 시간 -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/today_time_tv"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    tools:text="@string/friend_today_time" />
            </LinearLayout>

        </com.google.android.material.card.MaterialCardView>

        <!-- 사진 인증 버튼 -->
        <TextView
            android:id="@+id/photo_verify_btn"
            android:layout_width="95dp"
            android:layout_height="32dp"
            android:layout_marginStart="8dp"
            android:background="@drawable/rounded_box_lightblue"
            android:elevation="4dp"
            android:gravity="center"
            android:paddingHorizontal="16dp"
            android:text="@string/friend_photo_verification_btn"
            android:textColor="@android:color/black"
            android:textSize="14sp"
            app:layout_constraintStart_toEndOf="@id/today_time_cv"
            app:layout_constraintTop_toBottomOf="@id/friend_profile_ll" />

    </LinearLayout>


</androidx.constraintlayout.widget.ConstraintLayout>